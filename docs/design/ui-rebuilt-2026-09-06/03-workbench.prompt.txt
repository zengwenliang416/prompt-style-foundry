Generate a new reconstructed desktop UI design, exactly 3840x2160 pixels (4K UHD), high quality. No reference image is attached: the originals were lost. Reconstruct from the page description below, not an image edit. Retain the warm ivory paper background, dark ink typography, restrained dark teal and amber, expressive Chinese serif headings, clean readable Simplified Chinese body text and fine engineering blueprint line accents. Preserve the specified sidebar/main/rail and panel hierarchy, adapting spacing to 16:9 without stretching. This is a full flat viewport with no device frame, no perspective, no clipping, no purple. Use a consistent 280px-equivalent navigation sidebar and generous editorial spacing at a 1920 CSS-pixel viewport rendered at 2x; body text at least 32 actual pixels. Draw rich original artwork thumbnails and precise icons; do not fill the page with meaningless measurement marks. This is a reconstructed design concept of the direct-BYOK mode, not a live implementation or evidence of existing accounts or backend. No fake activity. Render only one finished full-page design image.

Product: 一图万式 / OnePic Template Studio. A static visual prompt template library with local browser BYOK generation. Exactly one image is used with each compiled public template, no required text input. The ORIGINAL blueprint classification is 文生图蓝图 or 图生图蓝图, NOT two selectable rendering endpoints for the compiled single-image flow. User explicitly triggers a request directly to their configured endpoint. No accounts, subscriptions, pricing, team/cloud storage, notifications, social usage counters, or server proxy. Do not invent these. Keep secret values masked. All proposed history/collection features are local browser concept features, not already implemented claims.

Use consistent five-link navigation: 总览 / 模板发现 / 生成工作台 / 我的工作区 / 使用指南. Brand is 一图万式. Show small unobtrusive badge 设计概念. Do not invent existing user activity; where demonstration content is needed, mark 示例. English micro labels are optional and subordinate to Chinese.

Page 3: 生成工作台. Reconstruct the documented three-column input / large result canvas / prompt inspector and bottom action bar. Input exactly one image, visually show a camera photo thumbnail marked 示例参考图, its dimensions 3000x2000. Center shows same camera rendered as refined editorial blueprint marked 效果示意, not nonexistent completed user result. Prompt inspector tabs 单图模板 / 示例实际提示词. Readable sample starts [System / Prompt], then 仅使用上传的一张图片 / 不追问，不要求填写文字 / 保持输入图片比例与方向 / BEGIN VISUAL BLUEPRINT / 工程蓝图视觉处理 / END VISUAL BLUEPRINT. Label blueprint 图生图蓝图 and explain 原始分类，不改变单图输入规则. Bottom: model gpt-image-2, 质量 高, 比例 继承参考图, 配置接口, primary 生成图片, secondary 下载结果. No seed, arbitrary clarity sliders, invented style controls or fabricated token estimate. Explicit line 点击生成后，图片与提示词直连你配置的接口. Workbench active.
