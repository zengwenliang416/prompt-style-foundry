Create a high-fidelity 3072x2048 desktop website design from the supplied reference image. This is a refinement of the same page, not a new unrelated style and not a bitmap upscale: redraw typography, thumbnails, controls and fine lines at requested resolution. Keep warm ivory, dark ink, restrained teal and amber, editorial Chinese serif headings, clean Chinese body text, fine blueprint accents. Reduce noisy measurement marks and clutter, improve whitespace and contrast, sharp readable Simplified Chinese, 24px equivalent body at this 2x desktop scale. Full flat viewport, no device frame, no perspective, no clipping, no purple. Preserve the broad page composition while correcting product inaccuracies below.

Product: 一图万式 / OnePic Template Studio. A static visual prompt template library with local browser BYOK generation. Exactly one image is used with each compiled public template, no required text input. The ORIGINAL blueprint classification is 文生图蓝图 or 图生图蓝图, NOT two selectable rendering endpoints for the compiled single-image flow. User explicitly triggers a request directly to their configured endpoint. No accounts, subscriptions, pricing, team/cloud storage, notifications, social usage counters, or server proxy. Do not invent these. Keep secret values masked. All proposed history/collection features are local browser concept features, not already implemented claims.

Use consistent five-link navigation: 总览 / 模板发现 / 生成工作台 / 我的工作区 / 使用指南. Brand is 一图万式. Show small unobtrusive badge 设计概念. Do not invent existing user activity; where demonstration content is needed, mark 示例. English micro labels are optional and subordinate to Chinese.

Page 5: 使用指南与来源. Preserve reference educational diagram and lower three columns but FIX incorrect prohibition of image-to-image: there must be NO 禁止直接图生图 message. Headline 图片决定内容，蓝图决定风格. Main diagram flows horizontally in sensible order: 上传一张图片 + 选择视觉蓝图 -> 生成结果, camera photo and camera blueprint visual. Show priority 单图规则 > 模板视觉蓝图 > 示例内容. Left lower 来源信息: 模板编号 / 作者署名 / 上游链接 / MIT 许可; no invented camera EXIF/person/private file details. Center 提示词结构 with [System / Prompt], BEGIN VISUAL BLUEPRINT and END VISUAL BLUEPRINT. Right muted teal guidance card 文生图与图生图：这里标记的是原始蓝图类型，公开模板均使用一张上传图片，无需填写文字. Footer 数据只在你点击生成后直连自定义接口. Mention preferred Nano Banana Pro when available without implying only renderer. No studio versioning/project server tools. Guide active.

